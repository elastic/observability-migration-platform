# Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one or more contributor license agreements.
# SPDX-License-Identifier: Elastic-2.0

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

  prometheus:
    config:
      scrape_configs:
        - job_name: node-exporter
          scrape_interval: 15s
          static_configs:
            - targets: ["node-exporter:9100"]
        - job_name: cadvisor
          scrape_interval: 15s
          static_configs:
            - targets: ["cadvisor:8080"]
        - job_name: prometheus
          scrape_interval: 15s
          static_configs:
            - targets: ["prometheus:9090"]
        - job_name: nginx-exporter
          scrape_interval: 15s
          static_configs:
            - targets: ["nginx-exporter:9113"]
        - job_name: otel-collector-self
          scrape_interval: 15s
          static_configs:
            - targets: ["localhost:8888"]

processors:
  batch:
    timeout: 5s
    send_batch_size: 1000

extensions:
  health_check:
    endpoint: 0.0.0.0:13133

exporters:
  prometheusremotewrite:
    endpoint: "http://prometheus:9090/api/v1/write"
    resource_to_telemetry_conversion:
      enabled: true

  prometheus:
    endpoint: "0.0.0.0:8889"
    namespace: otel

  # The dedicated `loki` exporter was removed from collector-contrib after
  # v0.120. Loki 3.x ingests OTLP natively, so logs now go over OTLP/HTTP;
  # the exporter appends /v1/logs to this endpoint.
  otlphttp/loki:
    endpoint: "http://loki:3100/otlp"

  elasticsearch:
    endpoints: ["http://elasticsearch:9200"]
    mapping:
      mode: otel

  debug:
    verbosity: basic

service:
  extensions: [health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [elasticsearch, debug]
    metrics:
      receivers: [otlp, prometheus]
      processors: [batch]
      exporters: [prometheus, elasticsearch]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/loki, elasticsearch]
