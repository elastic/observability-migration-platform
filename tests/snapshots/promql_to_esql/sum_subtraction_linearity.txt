source: sum(node_memory_MemTotal_bytes{cluster="$cluster",job="$job"} - node_memory_MemAvailable_bytes{cluster="$cluster",job="$job"})
feasibility: feasible
warning: Approximated PromQL arithmetic using same-bucket ES|QL math
warning: Dropped variable-driven label filters during migration
warning: Target field capabilities were unavailable, so the metric type of 'node_memory_MemTotal_bytes' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel will fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields). Pin 'metric_kinds: node_memory_MemTotal_bytes: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
warning: Target field capabilities were unavailable, so the metric type of 'node_memory_MemAvailable_bytes' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel will fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields). Pin 'metric_kinds: node_memory_MemAvailable_bytes: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
warning: PromQL series labels were not retained; output is bucket-level and may collapse multiple source series
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_memory_MemTotal_bytes IS NOT NULL OR node_memory_MemAvailable_bytes IS NOT NULL
| STATS node_memory_MemTotal_bytes_cluster_job_sum = SUM(node_memory_MemTotal_bytes), node_memory_MemAvailable_bytes_cluster_job_sum = SUM(node_memory_MemAvailable_bytes) BY time_bucket = TBUCKET(5 minute)
| EVAL computed_value = (node_memory_MemTotal_bytes_cluster_job_sum - node_memory_MemAvailable_bytes_cluster_job_sum)
| KEEP time_bucket, computed_value
| SORT time_bucket ASC
