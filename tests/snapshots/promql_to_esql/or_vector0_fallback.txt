feasibility: feasible
warning: Collapsed all series of `up` into a single AVG line; the source selector has no series labels (no legend, by(), or dashboard reference), so per-series detail is dropped. Add a legend/by() or migrate with target access to recover per-series fidelity.
warning: PromQL 'or' fallback: using left operand only; right-hand side metric ignored
---
FROM metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE service.name == "prom"
| WHERE up IS NOT NULL
| STATS up = AVG(up) BY time_bucket = BUCKET(@timestamp, 50, ?_tstart, ?_tend)
| SORT time_bucket ASC
