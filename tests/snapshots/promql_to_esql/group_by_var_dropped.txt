source: sum(kube_pod_info) by (namespace, label_Env)
feasibility: feasible
warning: Target field capabilities were unavailable, so the metric type of 'kube_pod_info' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel will fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields). Pin 'metric_kinds: kube_pod_info: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE kube_pod_info IS NOT NULL
| STATS kube_pod_info = SUM(kube_pod_info) BY time_bucket = TBUCKET(5 minute), k8s.namespace.name
| SORT time_bucket ASC
