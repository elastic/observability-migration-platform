source: sum(node_memory_MemFree_bytes + node_memory_Cached_bytes) by (instance)
feasibility: feasible
warning: Approximated PromQL arithmetic using same-bucket ES|QL math
warning: Target field capabilities were unavailable, so the metric type of 'node_memory_MemFree_bytes' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel may fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields); most gauge fields are unaffected. Pin 'metric_kinds: node_memory_MemFree_bytes: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
warning: Target field capabilities were unavailable, so the metric type of 'node_memory_Cached_bytes' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel may fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields); most gauge fields are unaffected. Pin 'metric_kinds: node_memory_Cached_bytes: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_memory_MemFree_bytes IS NOT NULL OR node_memory_Cached_bytes IS NOT NULL
| STATS node_memory_MemFree_bytes_sum = SUM(node_memory_MemFree_bytes), node_memory_Cached_bytes_sum = SUM(node_memory_Cached_bytes) BY time_bucket = TBUCKET(5 minute), service.instance.id
| EVAL computed_value = (node_memory_MemFree_bytes_sum + node_memory_Cached_bytes_sum)
| KEEP time_bucket, service.instance.id, computed_value
| SORT time_bucket ASC
