source: 0.5 < node_load1
feasibility: feasible
warning: Approximated PromQL arithmetic using same-bucket ES|QL math
warning: Collapsed all series of `node_load1` into a single AVG line; the source selector has no series labels (no legend, by(), or dashboard reference), so per-series detail is dropped. Add a legend/by() or migrate with target access to recover per-series fidelity.
warning: PromQL series labels were not retained; output is bucket-level and may collapse multiple source series
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_load1 IS NOT NULL
| STATS node_load1 = AVG(LAST_OVER_TIME(node_load1)) BY time_bucket = TBUCKET(5 minute)
| EVAL computed_value = CASE(0.5 < node_load1, node_load1, NULL)
| WHERE computed_value IS NOT NULL
| KEEP time_bucket, computed_value
| SORT time_bucket ASC
