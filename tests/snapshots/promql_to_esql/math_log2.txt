feasibility: feasible
warning: Collapsed all series of `node_memory_usage` into a single AVG line; the source selector has no series labels (no legend, by(), or dashboard reference), so per-series detail is dropped. Add a legend/by() or migrate with target access to recover per-series fidelity.
warning: log2() translated via exact ES|QL equivalent
---
FROM metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_memory_usage IS NOT NULL
| STATS node_memory_usage = AVG(node_memory_usage) BY time_bucket = BUCKET(@timestamp, 50, ?_tstart, ?_tend)
| EVAL node_memory_usage = LOG(2, node_memory_usage)
| SORT time_bucket ASC
