source: quantile(0.95, node_filesystem_avail_bytes) by (job)
feasibility: feasible
warning: Target field capabilities were unavailable, so the metric type of 'node_filesystem_avail_bytes' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel may fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields); most gauge fields are unaffected. Pin 'metric_kinds: node_filesystem_avail_bytes: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_filesystem_avail_bytes IS NOT NULL
| STATS node_filesystem_avail_bytes = PERCENTILE(node_filesystem_avail_bytes, 95) BY time_bucket = TBUCKET(5 minute), service.name
| SORT time_bucket ASC
