source: sum(increase(rabbitmq_message_size_bytes_bucket{le="100.0"}[5m]) * on(instance, job) group_left(rabbitmq_cluster) rabbitmq_identity_info) / sum(increase(rabbitmq_message_size_bytes_bucket{le="+Inf"}[5m]) * on(instance, job) group_left(rabbitmq_cluster) rabbitmq_identity_info)
feasibility: feasible
warning: Dropped group_left label-enrichment join on 'rabbitmq_identity_info' (assumed a constant-1 label-only metric by naming convention) and aggregated the primary metric alone. Primary series with no matching join partner are kept — PromQL would drop them — so counts and totals may differ; verify the partner is truly a label-only metric.
warning: Approximated PromQL arithmetic using same-bucket ES|QL math
warning: PromQL series labels were not retained; output is bucket-level and may collapse multiple source series
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE rabbitmq_message_size_bytes_bucket IS NOT NULL
| STATS rabbitmq_message_size_bytes_bucket_le_100_0_increase_sum = SUM(CASE((le == "100.0"), INCREASE(rabbitmq_message_size_bytes_bucket), NULL)), rabbitmq_message_size_bytes_bucket_le_Inf_increase_sum = SUM(CASE((le == "+Inf"), INCREASE(rabbitmq_message_size_bytes_bucket), NULL)) BY time_bucket = TBUCKET(5 minute)
| EVAL computed_value = (rabbitmq_message_size_bytes_bucket_le_100_0_increase_sum / rabbitmq_message_size_bytes_bucket_le_Inf_increase_sum)
| KEEP time_bucket, computed_value
| SORT time_bucket ASC
