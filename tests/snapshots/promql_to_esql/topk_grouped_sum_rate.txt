source: topk(10, sum(rate(http_requests_total[5m])) by (handler))
feasibility: feasible
warning: Translated topk() as time-series breakdown by handler; ES|QL has no subquery support so all series are shown (top-10 filtering approximated)
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE http_requests_total IS NOT NULL
| STATS value = SUM(RATE(http_requests_total)) BY time_bucket = TBUCKET(5 minute), handler
| SORT time_bucket ASC
