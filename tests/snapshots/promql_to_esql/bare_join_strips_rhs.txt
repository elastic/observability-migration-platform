source: node_hwmon_temp_celsius * on(chip) group_left(chip_name) node_hwmon_chip_names
feasibility: feasible
---
FROM metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE node_hwmon_temp_celsius IS NOT NULL
| STATS node_hwmon_temp_celsius = AVG(node_hwmon_temp_celsius) BY time_bucket = BUCKET(@timestamp, 50, ?_tstart, ?_tend), chip, chip_name
| SORT time_bucket ASC
