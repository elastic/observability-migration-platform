source: http_requests_total{status="400"} or ignoring() http_requests_total{status="500"}
feasibility: feasible
warning: Counter referenced without rate(); using LAST_OVER_TIME to preserve raw cumulative value
warning: Rewrote PromQL set-or between same metric as a unified WHERE OR clause
---
TS metrics-*
| WHERE @timestamp >= ?_tstart AND @timestamp <= ?_tend
| WHERE (status == "400" OR status == "500")
| WHERE http_requests_total IS NOT NULL
| STATS http_requests_total_status_400 = MAX(LAST_OVER_TIME(http_requests_total)) BY time_bucket = TBUCKET(5 minute), status
| EVAL computed_value = http_requests_total_status_400
| KEEP time_bucket, status, computed_value
| SORT time_bucket ASC
