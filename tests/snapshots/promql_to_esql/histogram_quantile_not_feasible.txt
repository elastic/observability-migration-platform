source: histogram_quantile(0.9, rate(alertmanager_notification_latency_seconds_bucket[5m]))
feasibility: not_feasible
warning: histogram_quantile over a classic _bucket series requires the le label in the aggregation (e.g. sum by (le)); without it the bucket boundaries are lost or per-series breakdown cannot be preserved, so this requires manual redesign
---

