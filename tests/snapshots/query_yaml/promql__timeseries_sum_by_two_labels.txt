source: sum(kube_pod_info) by (namespace, pod)
status: migrated_with_warnings
chart_type: line
dimension: time_bucket
breakdown: series_group
metrics: ['kube_pod_info']
warning: Target field capabilities were unavailable, so the metric type of 'kube_pod_info' could not be verified. If it is stored as counter_long/counter_double in Elasticsearch, this panel may fail with a verification_exception (ES|QL forbids standard aggregations such as SUM/MAX/MIN/AVG on counter fields); most gauge fields are unaffected. Pin 'metric_kinds: kube_pod_info: counter' (or gauge) in the rule pack, or re-run with target field capabilities reachable, to resolve this.
warning: Composited multi-label grouping (k8s.namespace.name, k8s.pod.name) into a single XY breakdown column
