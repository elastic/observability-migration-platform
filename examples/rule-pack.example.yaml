# Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one or more contributor license agreements.
# SPDX-License-Identifier: Elastic-2.0

# Example declarative rule pack for the Grafana migration CLI

query:
  # Fallback range window for rate()/increase()/*_over_time() when the source
  # query has none, and the window that Grafana's dynamic interval macros
  # ($__interval / $__rate_interval / $__auto_interval_* / $interval) collapse
  # to (ES|QL has no render-time auto interval). $__range keeps its own 1h.
  default_rate_window: 5m
  default_gauge_agg: AVG
  logs_index: logs-*

  label_rewrites:
    cluster: service.name

  label_candidates:
    instance:
      - service.instance.id
      - host.name
    job:
      - service.name
    node:
      - k8s.node.name
      - host.name

  ignored_labels:
    - origin_prometheus

  # Optional source-metric → target-field map (shared with Datadog profiles).
  # Author for your environment; class-2 transform/attribute_filter/unit_scale
  # entries are recorded as gaps in v1 (never silent bare renames).
  # metric_map:
  #   container_memory_working_set_bytes: container.memory.working_set

panel:
  type_map:
    table-old: datatable

  skip_types:
    - canvas

controls:
  field_overrides:
    job: service.name
    instance: service.instance.id
