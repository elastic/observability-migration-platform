# Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one or more contributor license agreements.
# SPDX-License-Identifier: Elastic-2.0

# Example declarative field profile for the Datadog migration CLI

name: custom_otel_serverless
metric_index: metrics-*
logs_index: logs-*
log_index_map:
  main: logs-generic-default
timestamp_field: "@timestamp"
metrics_dataset_filter: ""
logs_dataset_filter: ""

metric_map:
  system.cpu.user: system.cpu.user.pct
  system.mem.usable: system.memory.actual.used.bytes
  trace.flask.request.hits: trace.flask.request.hits
  nginx.requests.total_count: nginx.requests.total_count
  # Class-2 examples (Agent → OTel-style hazards) — synthetic names:
  # source.rate_as_gauge:
  #   target: target.pre_rated_gauge
  #   transform: drop_rate
  # source.cumulative_counter:
  #   target: target.bytes_total
  #   transform: to_rate
  # source.nanocores:
  #   target: target.cpu.usage
  #   unit_scale: 1.0e-9

tag_map:
  host: host.name
  env: deployment.environment
  service: service.name
  status: log.level
  kube_namespace: kubernetes.namespace
  datacenter: cloud.region

log_tag_map:
  host: host.name
  service: service.name
  status: log.level

metric_prefix: ""
metric_suffix: ""
tag_prefix: ""
